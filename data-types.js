// DATA TYPE is a type of a value, eg: string

let name = 'Tom'

// primitve data types has only ONE DATA TYPE

// ? string
// ? boolean
// ? number / integer
// ? undefined
// ? null
// ? bigInt
// ? symbol

// non-primitive data types have more than ONE DATA TYPE

// ? Array
// ? Object
// ? Regex
// ? Function
// ? Date
